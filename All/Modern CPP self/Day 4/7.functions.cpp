#include<iostream>
using std::cout;
#define line(msg)   cout<<"--------------(msg)--------------------\n";

int main()
{

    return 0;
}
// name mangling must be done on every function, tells which lang will consume it.
// tell conpiler follow convensions which that lang would have consumed

//constexpr before function name evaluate function at compile time.

//UDL : User defined literals,define and sell as per requirement
