## Auto keyword
- The `auto` keyword allows the compiler to automatically deduce the data type of a variable based on its initializer. This is particularly useful when dealing with complex or long type names, or when the type is not immediately obvious.
